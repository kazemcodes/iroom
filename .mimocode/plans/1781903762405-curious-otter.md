# Room Features: WebRTC Fix + Chat + Waiting Screen + Mobile UX

## Agent Competition Results

### Agent A: WebRTC Bug Investigation (11 bugs found)

| # | Severity | Bug | File:Line | Impact |
|---|----------|-----|-----------|--------|
| 1 | CRITICAL | Mutex deadlock in RemoveParticipant — `Conn.Close()` while holding lock triggers re-entrant lock | room.go:114-119 + signaling.go:220-224 | Any disconnect permanently freezes the room |
| 2 | CRITICAL | HandleLeave reads `c.Param("roomId")` but route param is `:id` | signaling.go:169 vs main.go:249 | Participants can never leave; ghost connections accumulate |
| 3 | CRITICAL | New joiners never receive existing participants' tracks (no catch-up sync) | signaling.go:227-258 | 2nd+ joiner never sees/hears earlier participants |
| 4 | CRITICAL | Join/leave notifications written as garbage JSON to RTP video tracks | signaling.go:333-340 | Real-time participant notifications completely broken |
| 5 | HIGH | Client ontrack uses StreamID (random) instead of ParticipantID | pion-client.ts:111 | Remote media state icons never update correctly |
| 6 | HIGH | shareScreen doesn't use "screen" track ID — server never detects screen sharing | pion-client.ts:169 | Screen share indistinguishable from camera |
| 7 | HIGH | fetchParticipants overwrites all local UI state every 5 seconds | +page.svelte:357-361 | Participant list flickers, local state lost |
| 8 | HIGH | HandleRoomInfo wrong param name + no route registered | signaling.go:195 + main.go | Room info endpoint unreachable |
| 9 | MEDIUM | OnICECandidate silently drops server-side candidates | signaling.go:214-218 | Fails behind symmetric NAT / with TURN |
| 10 | MEDIUM | DeleteRoom goroutine race window after RemoveParticipant | room.go:124-126 | New joiner can be flash-killed by cleanup goroutine |
| 11 | MEDIUM | MuteParticipant data race on IsMuted without lock | webrtc.go:94 | Mute state corrupted under concurrency |

### Agent B: Chat + Waiting Screen + Mobile UX Plan

#### Part 1: Chat TDD Plan
- **NEW**: `chat-service.ts` — Extractable, testable chat state/service module
- **NEW**: `chat-service.test.ts` — ~25 test cases covering: init, send, command, parseIncoming, history, reply, unread
- **MODIFY**: `+page.svelte` — Replace inline WebSocket code with chatService
- TDD cycle: RED (tests) → GREEN (implement) → REFACTOR (integrate)

#### Part 2: Waiting Screen
- **NEW**: `WaitingScreen.svelte` — Full-screen overlay for non-admitted users
- **NEW**: `WaitingListPanel.svelte` — Operator's waiting list with admit/reject
- **MODIFY**: `chat.go` — Add waiting room management via WebSocket
- **MODIFY**: `types.ts` — Add WaitingUser, WaitingRoomState interfaces
- WebSocket protocol: `waiting_update`, `waiting_action`, `waiting_result`

#### Part 3: Mobile UX
- **NEW**: `MobileToolbar.svelte` — Bottom toolbar (44px touch targets)
- **NEW**: `MobilePanelSheet.svelte` — Full-screen slide-up panels
- **MODIFY**: `+page.svelte` — Responsive CSS, swipe-to-close gesture
- Breakpoints: mobile <640px, tablet 641-1024px, desktop 1025px+
- Features: safe area insets, bottom toolbar, full-screen panels, swipe gestures

---

## Implementation Order

### Phase 1: Fix Critical WebRTC Bugs (Agent A bugs 1-4)
1. Fix mutex deadlock in RemoveParticipant
2. Fix HandleLeave param name
3. Add track catch-up sync for new joiners
4. Fix broadcastToRoom to use DataChannel instead of RTP

### Phase 2: Fix High WebRTC Bugs (Agent A bugs 5-8)
5. Pass user ID through signaling for track matching
6. Fix screen share track detection
7. Merge fetchParticipants instead of replacing
8. Fix HandleRoomInfo + register route

### Phase 3: Chat Implementation (Agent B Part 1)
9. Write chat-service.test.ts (RED)
10. Implement chat-service.ts (GREEN)
11. Integrate in +page.svelte (REFACTOR)

### Phase 4: Waiting Screen (Agent B Part 2)
12. Add types + backend waiting room logic
13. Create WaitingScreen.svelte + WaitingListPanel.svelte
14. Integrate WebSocket protocol

### Phase 5: Mobile UX (Agent B Part 3)
15. Create MobileToolbar.svelte + MobilePanelSheet.svelte
16. Add responsive CSS + swipe gestures
17. Test on mobile viewport

## Files to Create/Modify

| File | Action | Phase |
|------|--------|-------|
| `internal/webrtc/room.go` | FIX | 1 |
| `internal/webrtc/signaling.go` | FIX | 1-2 |
| `internal/adapter/handler/webrtc.go` | FIX | 2 |
| `web/src/lib/classroom/pion-client.ts` | FIX | 2 |
| `web/src/routes/room/[slug]/+page.svelte` | FIX/MODIFY | 1-5 |
| `web/src/lib/classroom/chat-service.ts` | CREATE | 3 |
| `web/src/lib/classroom/chat-service.test.ts` | CREATE | 3 |
| `web/src/lib/classroom/types.ts` | MODIFY | 4 |
| `web/src/lib/components/classroom/WaitingScreen.svelte` | CREATE | 4 |
| `web/src/lib/components/classroom/WaitingListPanel.svelte` | CREATE | 4 |
| `internal/adapter/handler/chat.go` | MODIFY | 4 |
| `web/src/lib/components/classroom/MobileToolbar.svelte` | CREATE | 5 |
| `web/src/lib/components/classroom/MobilePanelSheet.svelte` | CREATE | 5 |

## Verification
- All Go tests pass: `go test ./internal/... -count=1`
- All frontend tests pass: `npx vitest run src/lib/classroom/`
- Frontend builds: `npx vite build`
- Go builds: `go build ./cmd/...`
- Manual test: 2 browser tabs, verify video/audio sharing
- Manual test: Chat send/receive/reply
- Manual test: Mobile viewport (Chrome DevTools)
