# Frontend Tests & Cleanup Plan

## Phase 1: Cleanup — Remove Dead Code

### 1a. Remove orphaned components (24 files)

**Orphaned classroom components** (12 files in `src/lib/components/classroom/`):
- `BlockMenu.svelte` — old menu, replaced by AppMenu
- `ClassroomHeader.svelte` — replaced by inline header
- `EmojiPicker.svelte` — unused
- `MediaBlock.svelte` — unused
- `MiniToolbar.svelte` — replaced by TopMenu
- `Toolbar.svelte` — replaced by TopMenu
- `TopMenu.svelte` — replaced by inline nav bar
- `UserRow.svelte` — replaced by inline user rows
- `VideoGrid.svelte` — unused

**Orphaned top-level components** (3 files in `src/lib/components/`):
- `SettingsPopup.svelte` — replaced by SettingsModal
- `SkyroomHeader.svelte` — replaced by inline header
- `SkyroomSidebar.svelte` — replaced by inline sidebar

**Orphaned UI components** (12 files in `src/lib/components/ui/`):
- `SkyroomBadge.svelte`, `SkyroomButton.svelte`, `SkyroomEmptyState.svelte`, `SkyroomIcon.svelte`, `SkyroomInfoRow.svelte`, `SkyroomInput.svelte`, `SkyroomModal.svelte`, `SkyroomPageHeader.svelte`, `SkyroomSearch.svelte`, `SkyroomSpinner.svelte`, `SkyroomTabs.svelte`, `SkyroomToast.svelte`

### 1b. Remove orphaned store
- `src/lib/classroom/classroom-store.ts` — duplicates types in `classroom/types.ts`, unused

### 1c. Remove orphaned bridge
- `src/lib/classroom/ClassroomBridge.ts` — unused (classroom uses WebSocket now)

### 1d. Remove janus-gateway from package.json
- `janus-gateway` is in `dependencies` but no code imports it

### 1e. Remove @ts-nocheck from files
7 files with `@ts-nocheck`:
- `src/routes/(app)/admin/+page.svelte`
- `src/routes/(app)/admin/rooms/+page.svelte`
- `src/routes/(app)/admin/users/+page.svelte`
- `src/routes/(app)/chat/[id]/+page.svelte`
- `src/routes/(app)/classes/[id]/+page.svelte`
- `src/routes/classroom/join/[id]/+page.svelte`
- `src/routes/classroom/popup/[id]/+page.svelte`

**Strategy**: Remove `@ts-nocheck` one file at a time, fix TS errors, verify with `npm run check`. Start with simpler files first.

### 1f. Clean up console.log/console.error
- Keep `console.error` in classroom popup (useful for debugging join failures)
- Remove any `console.log` statements

## Phase 2: Test Infrastructure Setup

### 2a. Install Vitest
```
npm install -D vitest @testing-library/svelte @testing-library/jest-dom jsdom
```

### 2b. Configure Vitest in `vite.config.ts`
Add test config with jsdom environment.

### 2c. Add test script to package.json
```json
"test": "vitest run",
"test:watch": "vitest"
```

## Phase 3: Write Core Tests

### 3a. Store tests (`src/lib/stores.test.ts`)
- `auth.login()` — sets tokens in localStorage, updates state
- `auth.logout()` — clears tokens, resets state
- `auth.init()` — restores from localStorage
- `isAdmin` / `isTeacher` / `isStudent` derived stores

### 3b. API client tests (`src/lib/api.test.ts`)
- `api.get()` / `api.post()` / `api.put()` / `api.delete()` — correct URL, headers, token
- Error handling — 401 triggers logout, 429 handled
- Token attachment from localStorage

### 3c. Types tests (`src/lib/classroom/types.test.ts`)
- `ROLE_PERMISSIONS` — correct permissions per role
- `canMuteUser()` / `canKickUser()` — role hierarchy logic

### 3d. Classroom utils tests
- `ClassroomWindow.ts` — open/close tracking
- `pion-client.ts` — connect/disconnect lifecycle

## Verification

1. `npm run check` — 0 errors
2. `npm run test` — all tests pass
3. `npm run build` — builds successfully
4. Manual: classroom popup loads, join works, chat works

## Files Modified
- Delete: 24 orphaned components, 1 orphaned store, 1 orphaned bridge
- Edit: `package.json`, `vite.config.ts`, 7 files removing `@ts-nocheck`
- Create: `vitest.config.ts`, 4 test files
