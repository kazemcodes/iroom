localhost {
	encode gzip

	@api path /api/*
	@ws header Connection *Upgrade*

	handle @api {
		reverse_proxy app:8080
	}

	handle @ws {
		reverse_proxy app:8080
	}

	handle {
		reverse_proxy app:8080
	}
}
